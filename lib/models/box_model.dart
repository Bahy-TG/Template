import 'package:flutter/material.dart';

class BoxModel {
  final String text;
  final Color color;
  final width = 80;
  final height = 80;
  final radius = 10;

  BoxModel({required this.text, required this.color});
}
